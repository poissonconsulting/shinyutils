test_that("multiplication works", {
  # not sure why not work
#  expect_true(dev_prep())
#  expect_true(length(ls(all.names = TRUE, envir = globalenv())) == 0)
#  expect_true(is.null(names(sessionInfo()$otherPkgs)))
})
